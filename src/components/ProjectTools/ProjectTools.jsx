import "./ProjectTools.css";

const ProjectTools = ({ project }) => {
  console.log(project.tech)

  // Need to write something to lookup the proper icon

  // Need to write something in case user added a tech for which we don't have icon
  
  return (
    <div>ProjectTools</div>
  )
}

export default ProjectTools;